<%= form_for ([@user, @workout]) do |f| %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, :placeholder => 'Workout name' %><br />
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_field :date %><br />
  </div>
  <%= f.submit "Submit" %>

<% end %>
